# v1.3.0 Low-Contrast Specialist Workflow / 低对比度专项流程

This source update adds a Kaggle workflow for testing whether low-contrast-specific augmentation and loss choices improve segmentation on low-contrast images. It does not replace the default inference checkpoint unless the completed Kaggle results satisfy the replacement criteria.

本源码更新新增 Kaggle 低对比度专项流程，用于验证低对比度增强和 loss 选择是否能改善低对比度图像分割效果。除非 Kaggle 完整结果满足替换标准，否则不替换默认推理 checkpoint。

## Variants / 实验变体

- `control_bce_dice`: current high-accuracy recipe with BCE + Dice.
- `contrast_aug_bce_dice`: low-contrast augmentation with BCE + Dice.
- `contrast_aug_focal_dice`: low-contrast augmentation with Focal + Dice.
- `contrast_aug_tversky`: low-contrast augmentation with Tversky loss.

## Main Commands / 主要命令

Debug run:

```bash
python notebooks/kaggle_low_contrast_v1_3.py --debug
```

Full run:

```bash
python notebooks/kaggle_low_contrast_v1_3.py
```

If Kaggle stops at the GPU time limit, submit the same command again. Completed variants are skipped through `completed.json`, and unfinished variants resume from `checkpoints/last_model.pth` when available.

如果 Kaggle 因 GPU 时间限制停止，重新提交同一命令即可。已完成变体会通过 `completed.json` 跳过，未完成变体会在可用时从 `checkpoints/last_model.pth` 继续训练。

## Replacement Criteria / 替换默认模型标准

The default model should be replaced only if the best v1.3 variant satisfies all conditions below:

只有当最佳 v1.3 变体同时满足以下条件时，才建议替换默认模型：

- Internal test low-contrast Dice improves by at least `+0.02`, or low-contrast Recall improves by at least `+0.03`.
- Overall internal test Dice drops by no more than `0.01`.
- Visual samples do not show all-black masks, all-white masks, or large false-positive regions.

## Expected Outputs / 预期输出

```text
/kaggle/working/research_v1_3_low_contrast/comparison/low_contrast_comparison.csv
/kaggle/working/research_v1_3_low_contrast/comparison/low_contrast_comparison.md
/kaggle/working/research_v1_3_low_contrast/execution_manifest.json
/kaggle/working/release_artifacts/medical-segmentation-low-contrast-artifacts-v1.3.zip
```

## Reporting Policy / 结果报告原则

Do not fill numeric v1.3 results until the Kaggle workflow finishes and outputs are downloaded. Any future reported values must come from the generated CSV/Markdown artifacts.

在 Kaggle 流程完成并下载输出前，不填写 v1.3 数值结果。后续报告的任何数值都必须来自生成的 CSV/Markdown 产物。

## Medical Disclaimer / 医学免责声明

This project is intended only for medical image segmentation experiments and engineering workflow validation. It is not intended for clinical diagnosis, treatment recommendation, or real-world medical decision-making.

本项目仅用于医学图像分割算法实验和工程流程验证，不用于临床诊断、治疗建议或真实医疗决策。
