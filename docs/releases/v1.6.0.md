# v1.6.0 Project Completion Archive / 项目收尾归档

v1.6.0 concludes the planned engineering and Kaggle research series for this repository. The project is now maintenance-only. This archival release publishes source code, documentation, and curve histories only; it does not attach or replace model weights.

v1.6.0 完成本仓库计划内工程开发与 Kaggle 研究系列。项目现进入维护状态。本归档版本仅发布源码、文档和训练曲线历史，不附加或替换模型权重。

## Final Model Decision / 最终模型结论

The verified v1.0.0 U-Net++ EfficientNet-B3 checkpoint remains the default inference model:

已验证的 v1.0.0 U-Net++ EfficientNet-B3 checkpoint 继续作为默认推理模型：

```text
Release: v1.0.0
Config: configs/final_model.yaml
Recommended threshold: 0.35
SHA256: 4b04ccd5f4fbdad492a91ea9866d31b9329a886e74464ddf42fffa1854f76577
```

Neither v1.5 nor v1.6 passed the locked independent-test publication gate, so their checkpoints are not released as `fast`, `best_accuracy`, or default replacements.

v1.5 与 v1.6 均未通过锁定的独立测试发布门槛，因此其 checkpoint 不作为 `fast`、`best_accuracy` 或默认替代模型发布。

## Final Experimental Results / 最终实验结果

| Workflow | Variant | ISIC 2017 Test Dice | Test Boundary F1 | Low-Contrast Test Dice | ISIC 2018 External Dice | Published |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| v1.3 | `contrast_aug_bce_dice` | 0.864766 | 0.437615 | 0.832973 | 0.924386 | No replacement |
| v1.5 | `best_accuracy` | 0.858502 | 0.490880 | 0.827653 | 0.929573 | No |
| v1.6 | `fast` | 0.858215 | 0.489497 | 0.825290 | 0.932705 | No |
| v1.6 | `best_accuracy` | 0.857879 | 0.490239 | 0.821152 | 0.947575 | No |

The v1.6 `best_accuracy` ensemble improved external performance but remained below the locked test Dice gate of `0.884766`. This negative result is retained as part of the final research record.

v1.6 `best_accuracy` ensemble 改善了 external 表现，但仍低于锁定的 test Dice 门槛 `0.884766`。该未达标结果作为最终研究记录保留。

## Archive Contents / 归档内容

- Resumable v1.5 screening, five-fold teacher, OOF distillation, and ensemble workflow.
- Resumable v1.6 HAM10000 pretraining, target-domain teacher/student, and ten-teacher ensemble workflow.
- One-time locked test/external evaluations for v1.5 and v1.6.
- All 49 unique historical training curves with adjacent raw metric histories.
- Version-specific curve galleries and a central `docs/TRAINING_CURVES.md` index.
- Updated reports, completed experiment plan, maintenance policy, and final user guide.
- W&B disabled for the final v1.6 workflow.

- 可续训的 v1.5 架构筛选、五折 teacher、OOF 蒸馏与 ensemble 流程。
- 可续训的 v1.6 HAM10000 预训练、目标域 teacher/student 与十 teacher ensemble 流程。
- v1.5 与 v1.6 的一次性锁定 test/external 评估。
- 49 条唯一历史训练曲线及其相邻原始指标历史。
- 分版本曲线页面与统一的 `docs/TRAINING_CURVES.md` 总索引。
- 更新后的报告、已完成实验计划、维护策略和最终使用手册。
- 最终 v1.6 流程完全关闭 W&B。

## Validation / 验证

- Local test suite: `130 passed, 3 skipped`.
- Markdown asset/link audit: 49 curve files referenced exactly once, with no broken local links.
- GitHub Actions: Python 3.10 and Python 3.12 CPU jobs must pass before publication.
- No checkpoints, medical datasets, prediction caches, credentials, or W&B Secrets are included.

## Maintenance Policy / 维护策略

The repository accepts security fixes, dependency compatibility updates, reproducibility corrections, and critical bug fixes. New datasets, architectures, or Kaggle training require a separately approved project scope and are not part of the current backlog.

仓库仅继续接受安全修复、依赖兼容更新、可复现性修正和严重 bug 修复。新数据、新架构或 Kaggle 训练需要单独批准的新项目范围，不属于当前 backlog。

## Medical Disclaimer / 医学免责声明

This project is intended only for medical image segmentation experiments and engineering workflow validation. It is not intended for clinical diagnosis, treatment recommendation, or real-world medical decision-making.

本项目仅用于医学图像分割算法实验和工程流程验证，不用于临床诊断、治疗建议或真实医疗决策。
